"""Cross-module integration tests.

Place end-to-end tests that exercise multiple modules together here
(httpx AsyncClient against the FastAPI app with a real test Postgres).

Pure per-module tests live next to the module they exercise in
``src/modules/<feature>/tests/``.
"""
